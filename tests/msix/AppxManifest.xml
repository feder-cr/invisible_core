<?xml version="1.0" encoding="utf-8"?>
<!-- A minimal full-trust MSIX package for the msix-host job of ci.yml. It does
     nothing by itself: the job registers it in developer mode and runs
     host_probe.py inside its context with Invoke-CommandInDesktopPackage, so
     the probe's writes under AppData are redirected the way Claude Desktop's
     are. File system write virtualization is NOT disabled here on purpose;
     that redirection is the condition under test. probe.exe and logo.png are
     supplied by the job. -->
<Package xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
         xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
         xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
         IgnorableNamespaces="uap rescap">
  <Identity Name="InvisibleCoreMsixHost" Publisher="CN=InvisibleCoreMsixHost" Version="1.0.0.0" ProcessorArchitecture="x64"/>
  <Properties>
    <DisplayName>InvisibleCoreMsixHost</DisplayName>
    <PublisherDisplayName>invisible-core CI</PublisherDisplayName>
    <Logo>logo.png</Logo>
  </Properties>
  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.26100.0"/>
  </Dependencies>
  <Resources>
    <Resource Language="en-us"/>
  </Resources>
  <Applications>
    <Application Id="Probe" Executable="probe.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements DisplayName="InvisibleCoreMsixHost" Description="CI probe" BackgroundColor="transparent"
                          Square150x150Logo="logo.png" Square44x44Logo="logo.png" AppListEntry="none"/>
    </Application>
  </Applications>
  <Capabilities>
    <rescap:Capability Name="runFullTrust"/>
  </Capabilities>
</Package>
